{
  "name": "toolsite",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tinacms dev -c \"next dev\"",
    "build": "tinacms build --skip-cloud-checks && next build && node scripts/indexnow.mjs",
    "build:local": "tinacms build --local --skip-cloud-checks && next build && node scripts/indexnow.mjs"
  },
  "dependencies": {
    "@tinacms/cli": "^2.5.6",
    "gsap": "^3.15.0",
    "heic2any": "^0.0.4",
    "next": "14.2.5",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^3.11.174",
    "qrcode": "^1.5.4",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "tesseract.js": "^7.0.0",
    "tinacms": "^3.11.0",
    "xlsx": "^0.18.5"
  }
}
